<Window x:Class="LinkManager.Views.LinkManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LinkManager.Views"
        xmlns:vm="clr-namespace:LinkManager.ViewModels"
        mc:Ignorable="d"
        Title="Window_LinkManager" Height="800" Width="1200">
    <Window.DataContext>
        <vm:HostVM />
    </Window.DataContext>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MinWidth="200"/>
            <ColumnDefinition Width="5" />
            <ColumnDefinition Width="3*" MinWidth="300"/>
        </Grid.ColumnDefinitions>

        <!--Grid 0, 0-->
        <!--Host 파일-->
        <Grid Grid.Row="0" Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <!--제목-->
                <RowDefinition Height="*" />
                <!--리스트뷰-->
                <RowDefinition Height="Auto" />
                <!--버튼-->
            </Grid.RowDefinitions>

            <!--제목-->
            <TextBlock Grid.Row="0"
                       Text="Host Files"
                       FontWeight="Bold"
                       Margin="10,10,0,0" 
                       HorizontalAlignment="Stretch" />

            <!--리스트뷰-->
            <ListView Grid.Row="1"
                      x:Name="HostListView"
                      DisplayMemberPath="Name"
                      SelectionMode="Extended"
                      Margin="10,10,10,0"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch">
                <ListView.View>
                    <GridView AllowsColumnReorder="True">
                        <GridViewColumn Header="Name"
                                        DisplayMemberBinding="{Binding Name}" />
                        <GridViewColumn Header="Path"
                                        DisplayMemberBinding="{Binding Path}" />
                    </GridView>
                </ListView.View>
            </ListView>

            <!--Add/Remove 버튼-->
            <StackPanel Grid.Row="2" 
                        Orientation="Horizontal"
                        HorizontalAlignment="Left"
                        Margin="10,10,0,10">
                <Button Content="Add"
                        Width="80"
                        Height="30"
                        Margin="2" />
                <Button Content="Remove"
                        Width="80"
                        Height="30"
                        Margin="2" />
            </StackPanel>
        </Grid>
        <!--/Grid 0, 0-->

        <!--Grid 0, 1-->
        <!--그리드 스플리터-->
        <GridSplitter Grid.Row="0" Grid.Column="1" 
                      Width="5"
                      HorizontalAlignment="Stretch"
                      ResizeBehavior="PreviousAndNext"
                      ResizeDirection="Columns" />
        <!--/Grid 0, 1-->

        <!--Grid 0, 2-->
        <!--Link 파일-->
        <Grid Grid.Row="0" Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <!--제목-->
                <RowDefinition Height="*" />
                <!--리스트뷰-->
                <RowDefinition Height="Auto" />
                <!--버튼-->
            </Grid.RowDefinitions>

            <!--제목-->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0"
                        Text="Link Files"
                        FontWeight="Bold"
                        Margin="10,10,0,0"
                        HorizontalAlignment="Stretch" />
                <CheckBox Grid.Row="2" Grid.Column="1"
                          Content="Integrated Management"
                          FontWeight="Bold"
                          Margin="10,10,0,0"
                          HorizontalAlignment="Stretch" />
            </Grid>

            <!--리스트뷰-->
            <ListView Grid.Row="1"
              x:Name="LinkListView"
              ItemsSource="{Binding SelectedItem.LinkFiles_Rvt, ElementName=HostListView}"
              DisplayMemberPath="Name"
              SelectionMode="Extended"
              Margin="10,10,10,0"
              HorizontalAlignment="Stretch"
              VerticalAlignment="Stretch">
                <ListView.View>
                    <GridView AllowsColumnReorder="True">
                        <GridViewColumn Header="Name"
                                DisplayMemberBinding="{Binding Name}" />
                        <GridViewColumn Header="Status"
                                DisplayMemberBinding="{Binding Isloaded}" />
                        <GridViewColumn Header="Path"
                                DisplayMemberBinding="{Binding Path}" />
                        <GridViewColumn Header="Path Type"
                                DisplayMemberBinding="{Binding PathType}" />
                    </GridView>
                </ListView.View>
            </ListView>

            <!--Add/Remove 버튼-->
            <StackPanel Grid.Row="2"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            Margin="0,10,10,10">
                <Button Content="Add"
                            Width="80" 
                            Height="30"
                            Margin="2" />
                <Button Content="Remove"
                            Width="80"
                            Height="30"
                            Margin="2" />
            </StackPanel>
            <!--/Grid 0, 2-->
        </Grid>

        <!--Grid 1, (0~2)-->
        <Grid Grid.Row="1" Grid.ColumnSpan="3">
            <!--실행/취소 버튼-->
            <StackPanel Grid.Row="2"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            Margin="0,10,10,10">
                <Button Content="Apply"
                            Width="80"
                            Height="30"
                            Margin="2" />
                <Button Content="Cancel"
                            Width="80"
                            Height="30"
                            Margin="2" />
            </StackPanel>
        </Grid>
        <!--/Grid 1, (0~2)-->
    </Grid>
</Window>
